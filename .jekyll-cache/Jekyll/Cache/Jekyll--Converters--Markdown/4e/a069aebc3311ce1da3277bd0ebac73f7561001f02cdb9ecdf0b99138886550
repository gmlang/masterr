I"Ð<p>Use <code class="language-plaintext highlighter-rouge">library()</code> instead of <code class="language-plaintext highlighter-rouge">require()</code> because the former loads the package whereas the latter tries to load the package. You can read <a href="https://yihui.name/en/2014/07/library-vs-require/">Yihui Xieâ€™s post</a> to understand their differences in detail and when to use <code class="language-plaintext highlighter-rouge">require()</code>.</p>
:ET